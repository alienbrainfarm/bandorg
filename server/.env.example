# Google OAuth Configuration
# Get these from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Session Configuration
# Generate a random string for session encryption
SESSION_SECRET=your_random_session_secret_here

# Admin Configuration
# Email address for the primary admin user (automatically granted admin privileges)
ADMIN_EMAIL=your_admin_email@example.com

# Application Configuration
PORT=3001
NODE_ENV=production

# Development Note:
# 1. Copy this file to .env in the server directory
# 2. Replace all values with your actual credentials
# 3. Never commit the .env file to version control
# 4. For local development, set NODE_ENV=development